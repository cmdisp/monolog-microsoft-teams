{
    "name": "cmdisp/monolog-microsoft-teams",
    "description": "Monolog Handler for sending messages to Microsoft Teams using the Incoming Webhook connector",
    "license": "MIT",
    "require": {
        "php": "^8.1",
        "monolog/monolog": "^3.1",
        "ext-json": "*",
        "ext-curl": "*"
    },
    "require-dev": {
        "php-coveralls/php-coveralls": "^2.5",
        "phpunit/phpunit": "^9.5"
    },
    "autoload": {
        "psr-4": {
            "CMDISP\\MonologMicrosoftTeams\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "CMDISP\\MonologMicrosoftTeams\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "phpunit": "phpunit --colors=auto --coverage-text",
        "watch": "phpunit-watcher watch",
        "test": [
            "@phpunit"
        ]
    }
}
